node_modules
# Backend is built inside the Docker multi-stage build from src+pom;
# local build output must not pollute the context.
backend/target
backend/.mvn
console/node_modules
console/.vite
.git
.gitignore
*.log
.summa
.idea
.vscode
console/dist
.env
.env.*
.env.local
.env.production
.env.*.local
*.keystore
*.p12
*.jks
opencode.json
opencode.jsonc
**/AGENTS.md
**/.gitignore
# Exclude root lockfile — Dockerfile uses Maven only; console build gets its own context.
/package-lock.json
# Python tooling not needed in Docker builds
tools/
# Runtime log output
target/logs/
